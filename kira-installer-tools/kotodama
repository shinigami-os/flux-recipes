[meta]
name = kira-installer-tools
version = 1.2
description = "Tools required only inside the installer environment: partitioning, filesystem creation, bootloader, EFI boot management, networking, user/group tools"
license = MIT
size = 1

[source]

[deps]
build =
runtime = efivar efibootmgr grub parted e2fsprogs dosfstools shadow wpa_supplicant git kira-basic-tools

[build]
cflags =

%pre-build

%build

%post-build

%install
