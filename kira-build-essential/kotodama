[meta]
name = kira-build-essential
version = 1.0
description = "A basic C/C++ build toolchain (gcc, g++, make, libc headers) - Alpine has no package literally named build-essential like Debian does"
license = MIT
size = 1

[source]

[deps]
build =
runtime = gcc g++ make libc-dev

[build]
cflags =

%pre-build

%build

%post-build

%install
