[meta]
name = libsodium
version = 1.0.22
description = "Modern, portable cryptography library"
license = ISC
size = 5

[source]
url = git+https://github.com/jedisct1/libsodium#1.0.22
sha256 =

[deps]
build = gcc make pkgconf
runtime =

[build]
cflags = -O2 -pipe -march=x86-64-v2

%pre-build

%build
./configure --prefix=/usr --disable-static
make

%post-build

%install
make DESTDIR="$DESTDIR" install
